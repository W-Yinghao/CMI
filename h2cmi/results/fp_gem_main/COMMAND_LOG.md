# FP-GEM Command Log

- P12A CPU-only freeze generation: `python -m h2cmi.prepare_fp_gem_freeze`. The method, scope, statistical estimands, and interpretation grid were frozen before target performance observation.
- Pre-amendment smoke job `893415`: zero-result infrastructure failure because `/tmp` was not visible on the compute node.
- Pre-amendment smoke job `893416`: clean V100 exact-config source retrain; stopped on the overstrict unrecoverable P9 byte-hash gate before RCT/GEM/evaluation labels/metrics; accepted rows `0`.
- P12A source-provenance amendment: `python -m h2cmi.prepare_fp_gem_freeze`. Direct P9 row reuse is replaced by frozen same-checkpoint reruns of the four official controls; no scientific setting changed and no target performance had been observed.

- P12A smoke submission: `sbatch --parsable -p V100 --export=ALL,FP_GEM_REPO=/home/infres/yinwang/CMI_AAAI/.codex_p12_launch_5b71ee8 scripts/fp_gem_smoke.slurm`. Job `893433` left `squeue`; artifact/hash/hook/leakage gates returned `PASS`. No target performance metric was computed.
- P12B rejected submissions: V100 arrays `0-160%6`, `0-63%6`, and `0-31%6` each returned `QOSMaxSubmitJobPerUserLimit` before job creation; accepted rows `0`.
- P12B scheduler-only sharding freeze: six V100 stride-6 tasks cover 161 group indices and two A100 stride-2 tasks cover 28 group indices exactly. Runner/config/unit manifest and scientific settings are unchanged.
- P12B V100 submission: `sbatch --parsable -p V100 --array=0-5%6 --export=ALL,FP_GEM_REPO=/home/infres/yinwang/CMI_AAAI/.codex_p12_launch_5b71ee8,FP_GEM_GROUP=V100,FP_GEM_GROUP_COUNT=161,FP_GEM_GROUP_STRIDE=6 scripts/fp_gem_array.slurm`; array `893448`.
- P12B A100 submission: `sbatch --parsable -p A100 --array=0-1%2 --export=ALL,FP_GEM_REPO=/home/infres/yinwang/CMI_AAAI/.codex_p12_launch_5b71ee8,FP_GEM_GROUP=A100,FP_GEM_GROUP_COUNT=28,FP_GEM_GROUP_STRIDE=2 scripts/fp_gem_array.slurm`; array `893453`.
- Array element `893448_0` / job `893449` failed before an accepted result because loading the smoke checkpoint did not restore custom SPD batch-normalization source-domain buffers. Its stdout, empty stderr, failed payload, and checkpoint were archived with checksums; accepted rows `0`.
- Exact V100 task-0 retry: `sbatch --parsable -p V100 --array=0-0%1 --export=ALL,FP_GEM_REPO=/home/infres/yinwang/CMI_AAAI/.codex_p12_launch_5b71ee8,FP_GEM_GROUP=V100,FP_GEM_GROUP_COUNT=161,FP_GEM_GROUP_STRIDE=6 scripts/fp_gem_array.slurm`; array `893456`. The retry fresh-trained and changed no runner, config, manifest, method, split, seed, or hyperparameter.
- Completion check: `squeue -h -j 893448,893453,893456 -o '%i|%T|%P|%M|%R'` returned no rows. The eight final result tasks have empty stderr, `shard_complete` stdout, 189/189 parsed `status=ok` unit payloads, and 1,134/1,134 result rows with no duplicate keys.
- Frozen analyzer: `python -m h2cmi.analyze_fp_gem --raw-root /home/infres/yinwang/.cache/h2cmi_training_caches/fp_gem_p12 --out-dir h2cmi/results/fp_gem_main --job-record h2cmi/results/fp_gem_main/fp_gem_submission_record.json`. A sandboxed invocation stopped before output because it could not resolve the Slurm controller; the scheduler-enabled invocation passed the `squeue` gate and generated the final packet.
- Independent red-team: `python h2cmi/results/fp_gem_main/fp_gem_final_red_team.py`; all 31 gates passed after independent seed averaging and 10,000-replicate bootstrap recomputation.
- Focused tests: `python -m pytest -q h2cmi/tests/test_fp_gem.py` could not start because the `icml` runtime has no `pytest` module. The same seven fixture-free `test_*` functions were imported and invoked directly; `7/7` passed.
