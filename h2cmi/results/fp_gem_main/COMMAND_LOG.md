# FP-GEM Command Log

- P12A CPU-only freeze generation: `python -m h2cmi.prepare_fp_gem_freeze`. No GPU was used. The method/config, 189-unit manifest, exact P9 source-state index, smoke gate, statistical estimands, and interpretation grid were frozen before target performance observation.
