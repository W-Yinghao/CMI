# C26 Q5 — predicted-class-mix label diagnostics (LABEL-DIAGNOSTIC-ONLY)

> Status **REQUIRES_REPERSISTENCE_REINFERENCE** — per-sample target labels not persisted; label diagnostics need the scoped re-persistence re-inference (label diagnostics QUARANTINED, never features).

Labels join only here (never in features); requires the scoped re-persistence re-inference.
