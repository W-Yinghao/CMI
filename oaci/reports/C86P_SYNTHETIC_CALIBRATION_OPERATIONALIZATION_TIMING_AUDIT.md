# C86P Synthetic Calibration Operationalization Timing Audit

The main C86P protocol fixed eleven required synthetic scenarios but did not
fully bind their RNG, field dimensions, generative laws, failure injections, or
validation targets. This additive object closes those execution semantics
before any registered synthetic draw or result.

At lock time:

```text
registered synthetic draws:       0
registered synthetic results:     0
real EEG or label access:          0
active-acquisition results:        0
```

The contract requires the future production active-policy dispatcher and all
2,048 chains. C86P shadow helpers cannot publish a registered result. No method,
budget, endpoint, population, threshold, taxonomy, or empirical claim changes.
