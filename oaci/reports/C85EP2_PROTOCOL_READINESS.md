# C85EP2 Protocol Readiness

## Gate

```text
C85E_FROZEN_FIELD_POLICY_USE_GEOMETRY_AND_ROBUST_RISK_PROTOCOL_LOCKED_READY_FOR_PI_AUTHORIZATION
```

The original C85E protocol and the additive C85EP2 semantics object are both
operative. The additive object changes no population, method, grid, endpoint,
or claim; it separates raw utility geometry from standardized decision risk and
fixes action identity, collapse scopes, target aggregation, and empirical CVaR.

## Input Acceptance

The C85U acceptance certificate passed exact authorization, lifecycle, U1,
U2, and final-bundle replay. Complete coverage is 944 contexts, 76,464
candidate rows, 18,432 historical decision rows, 8,749,056 finite Q0 actions,
and 944 Q0 shards. Historical endpoint replay error and regime mismatch are
both zero.

The 1,955-object C85E registry is read-only and excludes every direct protected
data path. C85E can consume the accepted C85U utilities and frozen action/result
objects without reopening labels, logits, EEG, construction data, or Stage C.

## Implementation Readiness

The lock-bound path implements:

- exact and near policy-use separation with T3 limited to exact collapse;
- stochastic finite-Q0 action distributions without chain pseudoreplication;
- raw-gap candidate geometry on the locked epsilon/tau grids;
- applicability-aware rank/top-k/regret joins with no new p-values;
- target-equal mean, median, worst, quantile, and fractional-boundary CVaR;
- fail-closed T1-T7 empirical applicability labels;
- 26 exploratory result tables and one atomic final result bundle.

## Lock

```text
C85E_EXECUTION_LOCK.json SHA-256:
  a59062305b521973476e0d40236069eba7c9e149aeca9d3fe03c08a1ce106176

lock commit:
  48e177c9914003202cc75cefb4a98832ea8250c3

status:
  LOCKED_READY_FOR_DIRECT_PI_AUTHORIZATION_NOT_AUTHORIZED

resource envelope:
  cpu-high / 32 CPU / 128 GiB / 0 GPU / 2 hours / <= 2 GiB output
```

C85E, C86, active acquisition, new data/model zoos, and manuscript changes
remain unauthorized.
