# C84L1R1 Protocol Readiness

C84L1C job `895928` consumed the original authorization and entered the Lee
engineering path. It completed all three Lee training phases and 73 complete
instrumentation units, then failed on the 74th checkpoint because a float32
`z @ W.T + b` replay error of `1.239776611328125e-5` exceeded the locked
`1e-5` threshold. Target-y access, construction/evaluation/oracle access,
target scientific metrics and target-outcome decisions were all zero.

The failed root and partial manifest are preserved and nonreusable. The
additive repair changes only the 1040-term CPU/GPU float32 classifier
reconstruction tolerance from `1e-5` to `2e-5`. Saved-softmax, repeat-logit and
repeat-z checks remain at `1e-6`. Candidate identities, fixed source-support
deletion, training, views, subjects and scientific contracts are unchanged.

The replacement lock
`f9ebd88c72915bb41ba2d2d84a2a00c6748272021d48043c299bce52a1ad3813`
binds 125 runtime objects, 44 implementation files and five protocols. It
requires a fresh authorization record, a fresh content-addressed root and
retraining all 243 units. The repaired runtime replays every bound object and
fails before root creation when the new authorization record is absent.

Verification: 12/12 synthetic cases and 44/44 final red-team checks passed.
The CPU-only Slurm regressions passed: focused 175, C65 661, C23 1,072 and full
1,996. The cumulative suites have one explained finalized-C78F skip and three
historical C79 authorization-state deselections. All stderr files are empty,
and `squeue` reports no active C84L1R1 job.

Final gate:
`C84L1C_FLOAT32_REPLAY_REPAIRED_AND_RELOCKED_READY_FOR_FRESH_PI_AUTHORIZATION`.

A fresh direct `授权 C84L1C` statement is required. The authorization consumed
by job `895928` cannot be reused. C84F and C84S remain unauthorized.
