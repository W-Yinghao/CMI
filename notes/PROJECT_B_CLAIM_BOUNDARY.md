# Project B — Claim Boundary (Step-2 synthetic)

Human-readable companion to the machine-readable `claim_boundary.json` (generated by
`scripts/project_b_step2g_report.py` into `/tmp/project_b_step2g_report/`). Use this to keep the paper
and any downstream claims defensible. Primary support mode: **`nested_site_excess_q95`**. Primary posture:
**support-valid IDENTITY; no TTA under degenerate ACAR-harm.**

## Claimable
1. Default Project B v1 never selects `OFFLINE_TTA` when ACAR-harm calibration is degenerate/unavailable.
2. Nested source-site support **excess** calibration fixes the Step-2E all-refuse failure on R2.
3. Project B can allow support-valid IDENTITY while still refusing low-ESS targets.
4. OACI reason codes expose whether refusal came from support, ESS, ACAR degeneracy, or TTA evidence.

## NOT claimable
1. Source-only ACAR-harm is not shown to be generally identifiable.
2. v1 does not recover R2's raw TTA benefit (TTA blocked under harm-calibration degeneracy).
3. Support-valid identity is not shown accurate under concept shift (HF3 concept-degraded identity passes support).
4. The density support threshold alone does not catch H-OOD after nested widening; `LOW_ESS` catches only a subset.
5. No target-label-tuned thresholds; all thresholds are source-only and label-safe.

## Locked worlds (means over seeds; nested mode unless noted)
| world | strict | raw ΔTTA | base cov | nested cov | nested acc-bAcc | off-TTA | low-ESS refused | ACAR-harm |
|---|---|---|---|---|---|---|---|---|
| R2 | 0.855 | +0.071 | 0.00 | 0.833 | 0.880 | 0.00 | 2 | degenerate |
| HF3 | 0.568 | −0.071 | 0.00 | 0.600 | 0.569 | 0.00 | 7 | degenerate |
| H_OOD | 0.579 | −0.231 | 0.00 | 0.500 | 0.522 | 0.00 | 2 | degenerate |

## Known limitations
- R2 missed benefit (TTA blocked under degenerate ACAR-harm).
- HF3 concept-degraded identity can pass source-only support checks.
- H_OOD density threshold cleared after nested calibration; `LOW_ESS` catches only a subset.
