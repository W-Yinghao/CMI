# FSR_07 — Figure & Table Plan

**Project FSR — Phase 3A.** Every figure/table is backed by a frozen artifact already on the branch — no new experiment. Figures are to be generated CPU-only from the listed CSV/JSON. Provenance tier and caveat are part of each caption.

## Figure 1 — The FSR six-level ladder (schematic)
- **Content:** L1 detectability → L2 reducibility → L3 erasability → L4 task coupling → L5 functional reliance → L6 target consequence, with the predictor/endpoint split and the "no L1→L5 leap" rule. Annotate which RQ tests which edge.
- **Backing:** `FSR_02_METRIC_SCHEMA.md` (definitions), `FSR_00` (ladder).
- **Type:** hand-drawn schematic (no data).

## Figure 2 — RQ1/RQ3: alignment vs leakage → reliance
- **Panel A:** scatter `align_k2` vs `R3_task_drop` (n=126), pooled + colored by dataset; annotate ρ=+0.34 [+0.168,+0.504], **RECOMPUTED**.
- **Panel B:** scatter `graph_kl` vs `R3_task_drop` (seed0, n=42); annotate ρ=−0.42, **RECOMPUTED_SIGN_ONLY**; plus a marker for the frozen pooled −0.342 tagged **FROZEN_NOT_RECOMPUTABLE**.
- **Panel C:** the signed Spearman difference (align−graph_kl) = +0.816 [+0.219,+1.333] with the bootstrap distribution; excludes 0.
- **Backing:** `results/fsr_phase2b/rq1_leakage_vs_reliance.{csv,json}`, `rq3_alignment_mechanism.{csv,json}`, `rq3_unit_table.csv`.
- **Caption must carry:** the three provenance tiers; "alignment is closer to reliance than leakage, not a validated estimator; dataset-heterogeneous (2015 ns)."

## Figure 3 — RQ2: erasure strength vs target benefit
- **Panel A:** scatter `E_subject_removed` vs `target_bAcc_delta` across 40 cells, markers for task-collapse (INLP) and binary-harm (Lee/Cho EEGNet); a shaded band at Δ=0; annotate `benefit_claimable = 0/40`.
- **Panel B:** the sensitivity strip — ρ per subset (all, clean, excl-random_k, excl-INLP, excl-both, LEACE/RLACE-only, within-dataset-backbone), showing the **sign flip** on LEACE/RLACE-only (+0.54) → the negative association is a non-robust confound.
- **Backing:** `results/fsr_phase2b/rq2_erasure_vs_target.csv`, `results/fsr_phase2c/rq2_sensitivity_by_family.csv`.
- **Caption must carry:** "headline = 0/40 certified benefits; the negative correlation is NOT reported as a finding (Step 2C: not robust)."

## Figure 4 — RQ4: branch load (descriptive) + the missing instrument
- **Panel A:** bar chart of branch ablation drops + gate weights (graph/temporal/spatial × 2a/2015); spatial load-bearing.
- **Panel B:** a "measurement gap" table overlay — L1 per-branch leakage and L5 per-branch reliance marked ABSENT (`BLOCKED_MISSING_METRIC`).
- **Backing:** `results/fsr_phase2b/rq4_branch_load_descriptive.csv`, `rq4_branch_missing_metric_report.md`.
- **Caption must carry:** "spatial branch is load-bearing; branch-local leakage/reliance not measured — RQ4 blocked, not failed."

## Table 1 — Claim ledger (C1–C10)
- **Content:** claim, status (READY / READY_WITH_CAVEAT / SUPPORT_ONLY), ladder levels, evidence, mandatory caveat.
- **Backing:** `docs/FSR_05_CLAIM_LEDGER.md`, `results/fsr_phase2b/claim_readiness_table.csv`.

## Table 2 — Route inclusion / exclusion + target-label firewall
- **Content:** per route: cluster, predictor levels, endpoint levels, inclusion tag (INCLUDED / SUPPORT_ONLY / BOUNDARY_ONLY / PROTOCOL_ONLY / BACKGROUND_ONLY), `target_labels_used_for_fit`.
- **Backing:** `results/fsr_phase2/analysis_inclusion_table.csv`, `results/fsr_artifact_index/artifact_index.csv`.
- **Purpose:** shows exactly which 6/37 routes enter quantitative RQ tests and that no `YES_FORBIDDEN`/`AUDIT_ONLY` route does.

## Appendix figures/tables (optional)
- **A1:** RQ1 robustness (per-seed, per-method, LODO, rank-residualized) from `rq1_leakage_vs_reliance.csv`.
- **A2:** RQ2 full 40-cell table with `benefit_block_reason`.
- **A3:** RQ2 per-dataset/per-backbone sign table from `rq2_within_dataset_backbone.csv`.
- **A4:** provenance/branch inventory (SHA per artifact) from `branch_inventory.md` + `artifact_index.csv`.

## Rendering rules
- All figures generated by a CPU-only `scripts/fsr/plot_*.py` from the frozen CSVs (to be written in the drafting phase); no re-inference.
- Every caption states the provenance tier and the mandatory caveat from `FSR_05`.
- No figure asserts a forbidden claim (no "erasure improves DG", no "spatial leakage harmful", no "validated estimator").
