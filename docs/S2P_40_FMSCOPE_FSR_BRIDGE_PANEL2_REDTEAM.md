# S2P_40 - FMScope-FSR Bridge Panel 2 Red-Team

**Status:** FROZEN FAILURE MODES / NO SCIENTIFIC RESULT YET.

1. **Mutable checkpoint drift.** Recheck every content-addressed SHA before and after extraction; fail the object
   on any mismatch.
2. **Representation substitution.** The only Panel-2 feature is final channel-and-patch mean `[B,200]`. Do not
   switch to flattened features, target-fit PCA, a selected layer, or a favorable pooling rule.
3. **FACED clip leakage.** Preserve original clip IDs and pool only final probabilities. The three segments of a
   clip may not be split across fitting and scoring contracts.
4. **Global diagnostic relabelled deployment.** Global LEACE uses target features and IDs and is permanently
   marked transductive/cohort-conditioned.
5. **Target-label leakage.** Target task labels may score final predictions only. They cannot set rank, head,
   normalization, checkpoint, null, threshold, or protocol.
6. **Generic conditioning mistaken for identity specificity.** A benefit must exceed unchanged, same-rank random,
   and the removed-variance-matched sensitivity in the same direction.
7. **Fresh-head gain mistaken for repair.** Fresh and exact endpoints remain separate even when their signs agree.
8. **Transferable axis mistaken for utility.** Scatter removal and subject decode are transferability diagnostics,
   not task-benefit evidence.
9. **Task-gate bypass.** Exact-head mechanism language is forbidden below the frozen source-validation gate.
10. **Best-seed/budget selection.** Budget conclusions use paired seed means and the preregistered pooled-higher
    contrast; no strongest individual cell substitutes for it.
11. **SEED-V overclaim.** Its split is within cohort/session and cannot confirm unseen-subject deployment.
12. **ISRUC underpowered pooling.** ISRUC is per-test-subject directional evidence and cannot be pooled with other
    datasets or treated as an independent high-power family.
13. **Metric drift.** Kappa is primary utility; bAcc and NLL remain reported. No endpoint is swapped after results.
14. **Random-null instability.** Seeds are domain-separated and all draws persist. Any missing draw invalidates the
    affected cell; there is no replacement conditioned on its result.
15. **Phase-B authority rewrite.** Panel-2 pooled geometry does not overwrite the flattened-PCA128 Phase-B result.
16. **D1 auto-chain.** Panel-2 jobs never submit D1, downstream, fine-tuning, or any new training job.
